[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    network: tests that reach external networks/clouds (opt-in: pytest -m network)
    unit: fast, isolated unit tests (no chDB, no network)
    integration: tests that exercise a real chDB store or local services
